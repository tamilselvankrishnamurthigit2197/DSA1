// import React, {use} from 'react';

// import {useFormStatus} from 'react-router-dom'


/* function SubmitButton() {
    const { pending } = useFormStatus();
    return (
        <button>
            {pending ? 'Form Submitting': 'submit'}
        </button>
    )    
} */

/* export default function UseFormStatusExample() {
    async function handleSubmit(formData) {
        
        await new Promise((resolve)=> setTimeout(resolve, 3000));

        console.log("Form Submitted", formData);
    }
 */

    /*     return(
        <div>
            <h2 style={{marginBottom: 10}}> Use Form Status</h2>

            <form action={handleSubmit}>
                <div style={{display: 'flex', flexDirection: 'column'}}>
                    <label htmlFor="name of the user">
                    <input type="text" name="username" id="user2" />
                    </label>
                    <label htmlFor="password">
                    <input type="password" name="userpassword" id="pass2" />
                    </label>
                    <SubmitButton />
                </div>
            </form>
        </div>
    )
 */    
// } 